<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>헬스장 원정 도우미 GymEx Assist System</title>
    <!--부트스트랩 css 추가-->
    <link rel="stylesheet" href="/css/lib/bootstrap.min.css">

    <style>
        body {
            background-color: black;
            color: green;
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh; /* Centering vertically */
        }

        .grid-image, .h2-title {
            display: flex;
            justify-content: center;
        }

        img {
            margin: 0 0 5px;
            width: 70%;
            height: 550px;
        }

        .table th,
        .table td {
            justify-content: center;
            align-items: center;
            align-content: center;
            color: white;
        }
    </style>
</head>
<body>
<div class="container">
    <div>
        <h2 class="h2-title">검색 결과</h2>
    </div>

    <table class="table table-hover">
        <tr>
            <th>이름</th>
            <th>주소</th>
            <th>거리</th>
            <th>길안내 링크</th>
        </tr>
        {{#each outputFormList}}
            <tr>
                <td>{{gymName}}</td>
                <td>{{gymAddress}}</td>
                <td>{{distance}}</td>
                <td><a href="{{directionUrl}}">{{directionUrl}}</a></td>
            </tr>
        {{/each}}
    </table>
    <script src="/js/lib/jquery.min.js"></script>
    <script src="/js/lib/bootstrap.min.js"></script>
</div>
</body>
</html>
